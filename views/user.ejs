<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Express</title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <%- include ('blocks/header') -%>
    <h1>User's page: <%= username %></h1>
    <% if(username == 'jonh') { %>
    <ul>
        <% hobbies.forEach((element) => { %>
        <li><%= element %></li>
       <%   }) %>
    </ul>
    <% } %>
</body>
</html>